import * as React from "react";
import './AddNumberButton.sass';

export const AddNumberButton = (): JSX.Element => {
    return (
        <div className="add-number-wrapper">
            +
        </div>
    )
}